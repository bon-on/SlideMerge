# Slide Merge

A compact 5x5 sliding merge puzzle built with Flutter for iOS, Android, and web.

## Local Web Build

```sh
flutter build web --release --base-href /SlideMerge/
```

GitHub Pages deploys from `.github/workflows/deploy-pages.yml`.
